name: Compile test

on: [push, pull_request]

jobs:
  compile_test:
    runs-on: "ubuntu-latest"
    strategy:
      fail-fast: false
      matrix:
        config:
          - polserver-zip: "https://github.com/polserver/polserver/releases/download/NightlyRelease/Nightly-Linux-clang.zip"
          - polserver-zip: "https://github.com/KevinEady/polserver/releases/download/add-classes-0181f2e/Nightly-Linux-clang.zip"
    steps:
      - uses: actions/checkout@v2
      - name: Install Nightly ecompile
        run: |
          wget -q -O Nightly-Linux-clang.zip ${{ matrix.config.polserver-zip }}
          unzip Nightly-Linux-clang.zip
          rm -f *_dbg.zip
          rm -f Nightly-Linux-clang.zip
          unzip -j -o polserver*.zip
          echo "Unzip Complete"
          cp ecompile scripts/
          cp *.em scripts/
          echo "ModuleDirectory=scripts" >> scripts/ecompile.cfg
          echo "IncludeDirectory=scripts" >> scripts/ecompile.cfg
          echo "PolScriptRoot=scripts" >> scripts/ecompile.cfg
          echo "PackageRoot=pkg" >> scripts/ecompile.cfg
          echo "DisplaySummary=1" >> scripts/ecompile.cfg
          echo "DisplayWarnings=1" >> scripts/ecompile.cfg
          echo "CompileAspPages=1" >> scripts/ecompile.cfg
          sudo apt-get update
          sudo apt-get install clang-14 -y
      - name: Compile
        run: |
          for i in {1..2}; do time ./scripts/ecompile -A -b -T -f -q >/dev/null 2>&1; done
