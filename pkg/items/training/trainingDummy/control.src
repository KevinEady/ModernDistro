use uo;

program DummyControl(dummy)
  //De-animates dummies on start-up.

  if ( dummy.graphic == 4209) // Animated
    dummy.graphic := 4208; // None animated
    // Animated
    // Non animated
  elseif ( dummy.graphic == 4213)
    dummy.graphic := 4212;
  endif

  return 1;
endprogram
